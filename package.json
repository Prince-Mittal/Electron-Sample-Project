{
  "name": "sample-electron-project",
  "version": "1.0.0",
  "description": "\"Sample Electron Project\"",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-builder build --win --mac --publish never",
    "deploy": "electron-builder build --win --mac --publish always"
  },
  "author": "Prince Mittal",
  "license": "ISC",
  "dependencies": {
    "electron-updater": "^4.3.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Prince-Mittal/Electron-Sample-Project.git"
  },
  "devDependencies": {
    "electron": "^11.2.3",
    "electron-builder": "^22.9.1"
  }
}
